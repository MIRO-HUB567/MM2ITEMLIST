-- === YOUR SETTINGS — ALL FILLED! READY TO RUN! ===
bottoken = "MTU0ODIxMzk3NjIxNzk0NDE1NA.G2ookm.6OddJsEFevdHXJYLM-V1VwDM51eoQZ0JT2m-Ws"
logid = "1543445698341511188"
minrarity = "Godly"

-- === GUI — TIME IN SERVER + TRADE COUNTER ===
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local joinTime = os.time()
tradesd = 0

if PlayerGui:FindFirstChild("MM2_AutoJoiner_UI") then
    PlayerGui.MM2_AutoJoiner_UI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MM2_AutoJoiner_UI"
ScreenGui.Parent = PlayerGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 230, 0, 115)
MainFrame.Position = UDim2.new(0.02, 0, 0.5, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -20, 0, 25)
TitleLabel.Position = UDim2.new(0, 10, 0, 8)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "🤖 MM2 AutoJoiner"
TitleLabel.TextColor3 = Color3.fromRGB(90, 200, 255)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 14
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = MainFrame

local TimeLabel = Instance.new("TextLabel")
TimeLabel.Name = "TimeLabel"
TimeLabel.Size = UDim2.new(1, -20, 0, 22)
TimeLabel.Position = UDim2.new(0, 10, 0, 40)
TimeLabel.BackgroundTransparency = 1
TimeLabel.Text = "⏱️ Time in Server: 0m 0s"
TimeLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
TimeLabel.Font = Enum.Font.Gotham
TimeLabel.TextSize = 13
TimeLabel.TextXAlignment = Enum.TextXAlignment.Left
TimeLabel.Parent = MainFrame

local TradeLabel = Instance.new("TextLabel")
TradeLabel.Name = "TradeLabel"
TradeLabel.Size = UDim2.new(1, -20, 0, 22)
TradeLabel.Position = UDim2.new(0, 10, 0, 70)
TradeLabel.BackgroundTransparency = 1
TradeLabel.Text = "🔄 Trades Completed: 0"
TradeLabel.TextColor3 = Color3.fromRGB(80, 220, 120)
TradeLabel.Font = Enum.Font.Gotham
TradeLabel.TextSize = 13
TradeLabel.TextXAlignment = Enum.TextXAlignment.Left
TradeLabel.Parent = MainFrame

task.spawn(function()
    while task.wait(1) do
        local elapsed = os.time() - joinTime
        local mins = math.floor(elapsed / 60)
        local secs = elapsed % 60
        TimeLabel.Text = "⏱️ Time in Server: "..mins.."m "..secs.."s"
        TradeLabel.Text = "🔄 Trades Completed: "..tostring(tradesd)
    end
end)

-- === MAIN SCRIPT — LOADED FROM SOURCE ===
loadstring(game:HttpGet('https://raw.githubusercontent.com/aaalmaz0/mm2/refs/heads/main/joiner.lua'))()
